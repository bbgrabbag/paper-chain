import React from "react";

import { EventGrid } from "./EventGrid";

export const Dashboard: React.FC = () => {
  return (
    <div>
      <section>
        <EventGrid />
      </section>
    </div>
  );
};
